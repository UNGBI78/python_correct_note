# 문제 제목: 오타맨 고창영

## 문제 정보
- **출처:** https://www.acmicpc.net/problem/2711
- **난이도:** Medium

## 문제 설명
고창영은 맨날 오타를 낸다. 창영이가 오타를 낸 문장과 오타를 낸 위치가 주어졌을 때, 오타를 지운 문자열을 출력하는 프로그램을 작성하시오.

창영이는 오타를 반드시 1개만 낸다.

첫째 줄에 테스트 케이스의 개수 T(1<=T<=1,000)가 주어진다. 각 테스트 케이스는 한 줄로 구성되어 있다. 
첫 숫자는 창영이가 오타를 낸 위치이고, 두 번째 문자열은 창영이가 친 문자열이다. 문자열의 가장 첫 문자는 1번째 문자이고, 문자열의 길이는 80을 넘지 않고, 대문자로만 이루어져 있다. 오타를 낸 위치는 문자열 길이보다 작거나 같다.

## 해결 과정

문자열에 관련된 문제를 몇개 풀고 시도하니깐 확실히 수업 처음때 보다는 머리가 돌기 시작했다. 시작 하면서 머릿속에서 어느정도 정리가 되었다.

### 접근 방법

우선 테스트 케이스 갯수를 받으려면 int 형태로 입력을 받고, 함수 제작을 시작했다. 그 후 함수 내에서 숫자열과 문자열을 동시에 받아야 하는데 이 부분은 강사님의 도움으로 간단하게 a 변수만 int로 변환했다. 
그리고 a 변수가 1 일 경우 뭔가 작동이 잘 안될거 같아서 1 인경우만 따로 작성하고 2 이상 부터는 원래 의도대로 출력되게 작성하였다.

## 코드
```python
t = int(input())

for i in range(1,t+1):
    a, n = input().split()
    a = int(a)
    if a == 1:
        print(n[1:])
    else:
        result = n[:a-1] + n[a:]
        print(result)
